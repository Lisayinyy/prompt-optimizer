
  # prompt

  This is a code bundle for prompt. The original project is available at https://www.figma.com/design/AZpFmjpDS38QxDasthwdbe/prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  